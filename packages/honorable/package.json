{
  "name": "honorable",
  "version": "0.9.0",
  "description": "An extensible React component librarie for 2022 and beyond",
  "main": "dist/index.js",
  "scripts": {
    "dev": "npm run clean:peerDependencies && npm run clean:build && nodemon --watch src --exec \"npm run build\"",
    "build": "npx tsc",
    "clean": "npm run clean:build && npm run clean:node-modules && npm i && npm run clean:peerDependencies",
    "clean:node-modules": "rimraf node_module package-lock.json",
    "clean:peerDependencies": "rimraf node_modules/react node_modules/react-dom node_modules/@emotion",
    "clean:build": "rimraf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "\"git+https://github.com/dherault/honorable.git"
  },
  "keywords": [
    "React",
    "components",
    "librarie"
  ],
  "author": "David Hérault <dherault@gmail.com> (https://github.com/dherault)",
  "license": "MIT",
  "files": [
    "dist/**/*",
    "src/**/*"
  ],
  "dependencies": {
    "flexpad": "^3.0.0",
    "lodash.merge": "^4.6.2",
    "prop-types": "^15.8.1"
  },
  "peerDependencies": {
    "@emotion/react": "^11.9.0",
    "@emotion/styled": "^11.8.1",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15",
    "normalize.css": "^8.0.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.6.3"
  }
}
