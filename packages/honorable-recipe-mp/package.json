{
  "name": "honorable-recipe-mp",
  "version": "0.3.0",
  "description": "A spacing solution recipe for honorable",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "nodemon --watch src --watch tests --ext ts,tsx,snap --exec \"npm run test\"",
    "dev": "npm run clean:build && nodemon --watch src --ext ts,tsx --exec \"npm run build\"",
    "build": "npx tsc --declaration",
    "clean": "npm run clean:build",
    "clean:build": "rimraf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "keywords": [
    "honorable",
    "recipe"
  ],
  "author": "David Hérault <dherault@gmail.com> (https://github.com/dherault)",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.18.6",
    "@babel/preset-env": "^7.18.6",
    "@babel/preset-react": "^7.18.6",
    "@babel/preset-typescript": "^7.18.6",
    "@types/jest": "^28.1.4",
    "babel-jest": "^28.1.2",
    "jest": "^28.1.2",
    "nodemon": "^2.0.19",
    "rimraf": "^3.0.2",
    "ts-node": "^10.8.2",
    "typescript": "^4.7.4"
  }
}
